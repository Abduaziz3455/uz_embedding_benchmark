services:
  benchmark:
    build:
      context: .
      dockerfile: Dockerfile
      network: host
    image: uz-embedding-benchmark:cpu
    # Use host networking: the container doesn't expose ports, and this avoids
    # brittle iptables/bridge errors on some Linux hosts.
    network_mode: host
    volumes:
      - ./results_news:/app/results_news
      - ./dataset:/app/dataset
      - ./models_config.yaml:/app/models_config.yaml
      - ~/.cache/huggingface:/root/.cache/huggingface
    env_file:
      - path: .env
        required: false
    ipc: host
    command: ["list"]
